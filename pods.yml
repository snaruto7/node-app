apiVersion: v1
kind: Pod
metadata:
  name: nodeapp
  labels:
    app: nodeapp
spec:
  containers:
    - name: nodeapp
      image: snaruto7/nodeapp:tagVersion
      ports:
        - containerPort: 8080
